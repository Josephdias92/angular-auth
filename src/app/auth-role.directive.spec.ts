import { AuthRoleDirective } from './auth-role.directive';

describe('AuthRoleDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthRoleDirective();
    expect(directive).toBeTruthy();
  });
});
